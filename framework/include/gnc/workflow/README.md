# workflow

职责：Experiment、WorkflowDefinition/Plan、TaskDefinition、tool port、resource policy 和 research bundle。

允许依赖：foundation、contracts、evidence contracts、Application operation port。

禁止依赖：CycleFrame、CommittedStateStore、Runtime Cell pointer 和模型内部对象。
