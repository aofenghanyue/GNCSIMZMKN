# Gate decisions

阶段门决策采用 `G<id>-YYYY-MM-DD-<result>.md` 命名。记录只保存仓库所有者的 gate 结论、实际可运行证据、未解释科学差异、当前限制和解锁范围。

Bootstrap 验证结果保存在 `B0-<date>-passed.md`；产品阶段门按 `G<id>-<date>-<result>.md` 保存。当前 `G0-G1-2026-08-15-passed.md` 记录 R0 到 R1 的已接受结论。
