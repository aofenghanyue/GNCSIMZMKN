# R7｜Application Control、LLM 与 Blueprint Authoring

输入：Compiler schema/diagnostics、Application operations、Experiment 和 Artifact query。

输出：ResearchProposal、diff/approval、CommandLedger、Blueprint graph/source round-trip 和受控 LLM authoring。

Authoring 工具只能产生 proposal/source，所有执行继续经过 Compiler、policy 和 operation receipt。
