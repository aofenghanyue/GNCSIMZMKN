# R8｜Realtime 与前端适配

输入：Session command、committed snapshot、observation stream 和 backend capability。

输出：RenderSnapshot、command/snapshot transport、pacing、lag/drop policy、ImGui reference consumer，以及经 D-011 批准的其他 frontend demo。

当前默认状态为 PressureOnly。真实 consumer、性能预算和资源证据出现后，Product Owner 才能把具体子任务提升为 V1。
