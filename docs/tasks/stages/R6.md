# R6｜Experiment、Python 与 RL

输入：稳定 Plan、Session、Artifact 和 Workflow 边界。

输出：ParameterSpace、CaseMaterialization、worker backend、多 Session 隔离、Python reset/step、VectorEnv 和 TrainingProfile。

G9 要求 batch 与 Python 复用普通 Session，不把 case index、训练状态或 worker 调度塞入 Kernel。
